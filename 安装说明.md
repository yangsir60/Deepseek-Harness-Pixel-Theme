# 安装说明

面向使用者：怎么装、怎么切、怎么卸、怎么自己改配色重建。

**不需要 Node、不需要联网** —— 三套皮肤都是自包含的，字体随包分发。只有「想改配色重新构建」那一节才需要 Node。

---

## 一、安装

1. 把 `皮肤/` 下你要的那套（例如 `pixel-handheld/`）**整个文件夹**拷到皮肤的根目录：

   | 系统 | 皮肤根目录 |
   |---|---|
   | Windows | `C:\Users\<你>\.dsh\skins\` |
   | macOS / Linux | `~/.dsh/skins/` |

   拷完应该是 `…\.dsh\skins\pixel-handheld\skin.json` 这样，**注意不要多套一层文件夹**（`skins\pixel-handheld\pixel-handheld\` 是错的）。

2. **刷新页面**。皮肤的样式表与字体是页面加载时注入的，只切设置不刷新，看到的仍是旧皮肤。

3. 设置 → 皮肤中心 → 找到这套主题 → 应用。

三套可以同时装着，随时在皮肤中心里切换。

---

## 二、切换与卸载

- **切换**：皮肤中心里点另一套即可，不用重启、不用重装。
- **卸载**：把 `…\.dsh\skins\<皮肤 id>\` 整个文件夹删掉，然后刷新页面。

---

## 三、想改配色，重新构建

需要 Node（建议 18+）。

```bash
cd 源码
npm install                 # 只有一个 devDependency：puppeteer-core（截图工具用）

#  改配色：编辑 src/palettes/*.json —— 每个文件里含 dark 与 light 两套
#  改外观：编辑 src/css/{fonts,surfaces,patches}.css

node src/build.mjs          # 构建 → 源码/dist/
node tools/audit.mjs        # 配色与对比度校验（改配色后必跑）
node tools/verify.mjs       # 皮肤契约校验（判据：零错误）
node tools/shot.mjs <皮肤id>  # 可选：真实屏幕缩放下截图 + 探针
```

然后把 `源码/dist/<皮肤 id>/` 拷进皮肤根目录即可。

> **逐套配色验证时一定要显式传皮肤 id**。连着切多套皮肤时，皮肤 HTML 只在页面加载时注入一次，只有最后一套会真正渲染 —— 不指定 id 会让预览图与验证数字张冠李戴。

---

## 四、排错

| 现象 | 原因 / 处理 |
|---|---|
| 皮肤中心里看不到 | 层级多套了一层（`skins\<id>\<id>\`），或 `skin.json` 不在该文件夹根下 |
| 看到了但界面没变 | 没刷新页面；或只打开卡片没点「应用」 |
| 字体不是像素体 | `assets/fonts/` 没跟着拷过去，字体是随包分发的本地文件 |
| 颜色怪怪的、有的地方还是旧色 | 皮肤样式表是页面加载时注入的，**刷新**一次 |
| 想让自己的改动生效 | 改完源码要重新 `build` 并把 `dist/<id>/` 重新拷过去，再刷新 |

---

## 五、发给别人时的最小集

只要这几样就能装：

```
skin.json         清单，必须有
skin.css          token 层
patches.css       组件补丁层
assets/           字体（自包含，不联网）
```

再带上 `preview/`（皮肤中心里的预览图）、`NOTICE.txt` 与 `assets/fonts/LICENSE-OFL-font.txt`（**字体许可要求，必须保留**）。

---

## 六、本包目录结构

```
像素机架主题/
├── README.md                门面：这是什么、长什么样、怎么装
├── 安装说明.md              本文档
├── 主题制作与调优指南.md      想自己做主题看这份（可迁移的方法与踩坑记录）
├── LICENSE                  皮肤代码 MIT + 字体 OFL 说明
├── 皮肤/                    三套可直接安装的配色（自包含，无需构建）
│   ├── pixel-arcade/        街机霓虹
│   ├── pixel-handheld/      掌机绿
│   └── pixel-amber/         终端琥珀
├── 源码/                    生成器与验证工具链
│   ├── package.json
│   ├── src/                 build.mjs、token 表、调色板、CSS、字体
│   ├── tools/               校验与截图工具
│   └── preview-src/         构建预览图所需的源图
└── 预览/                    三套配色各明暗两张整屏截图
```
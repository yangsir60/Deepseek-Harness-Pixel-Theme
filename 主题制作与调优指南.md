# DSH 主题（皮肤）制作与调优指南

以「像素机架 Pixel Rig」为实例的一份实操手册。目标是**带着走**：换一台机器、换一套配色、换一个想改的地方，流程和坑都在这里，不需要重新摸索一遍。

> 安装、切换、卸载、改配色重建的步骤见 [安装说明.md](安装说明.md)；本主题是什么、长什么样见 [README.md](README.md)。

---

## 0. 先看清这件事的本质

DSH 的界面是**官方组件 + 你的主题样式表**两层叠出来的。所谓「换皮肤」，本质是在官方样式之上**抢回一部分控制权**。

于是整件事只剩下一个矛盾：**在同一个元素上，谁的样式最终生效。**

下文所有内容都围绕这一个矛盾展开 —— 认不清它，就会不断遇到「我明明写了却没效果」。

---

## 1. 三个必须先建立的概念

### 1.1 皮肤就是一个文件夹

```
~/.dsh/skins/<皮肤 id>/
├── skin.json         清单（v2）：id / name / 配色元信息 / contributes
├── skin.css          第一层：token 重映射（换颜色）
├── patches.css       第二层：组件补丁（换形状、间距、字体）
├── assets/fonts/     自带字体（自包含，不联网）
├── preview/          皮肤中心里的预览图
├── NOTICE.txt        来源与署名
└── LICENSE
```

Windows 下 `~/.dsh` 即 `C:\Users\<你>\.dsh`。把文件夹丢进 `skins/`，刷新后出现在「设置 → 皮肤中心」。

### 1.2 两条路线，能力天差地别

| | 声明式（Route B） | 脚本式 |
|---|---|---|
| 组成 | 只有 `skin.json` + 两份 CSS | 额外有 `hooks.mjs` 客户端脚本 |
| 能做什么 | 改颜色、字体、形状、间距 | 还能注入 DOM、改结构 |
| **手工放进 `skins/`** | **生效** | **`hooks.mjs` 不会被执行** |

**这条要记牢**：手工拷贝安装的皮肤，只有声明式 CSS 会生效。所以想做一个「发给别人就能装」的皮肤，就应该把能力限制在 CSS 内 —— 本项目就是这么做的，全部效果零脚本。

### 1.3 token 分三层，改层级越高覆盖越广

```
--dsw-static-*    最底层原始值（色阶、间距）
--dsw-alias-*     语义层（bg-base / label-primary / border-l1 …）
--dsw-specific-*  具体组件层（bubble / markdown-code-block …）
```

一套完整配色约 **138 个 token × 明暗两模式**。改造顺序应该是：先改 `static`（换色相），再按需覆盖 `alias`（修语义），最后才动 `specific`（调单点）。

> 皮肤加载器会把 `:root` 里的声明逐行克隆到 `scope body` 上（`scope` 是该皮肤的隔离层）。因此**声明必须一行一条**，且 `@import` 被禁用、`url()` 不允许 `http(s)://`、`//`、绝对路径或以 `../` 越界。

---

## 2. 唯一的铁律：加载顺序

**你的皮肤样式表，加载在官方样式表之前。**

CSS 的规则是：**特异性相同时，后出现的胜出**。官方在后，所以 ——

> **同特异性下，官方永远赢。**

这条铁律推导出全部操作要领：

### 2.1 什么时候必须 `!important`

不是所有属性都要，实测只有这几类绕不过去（官方用组件类名在更高特异性上声明了它们）：

| 属性 | 为什么要 `!important` |
|---|---|
| 按钮 `background-color` | 官方按钮组件自带底色 |
| `border` | 同上 |
| 所有 `border-radius` | 官方组件会用自己的类名声明圆角 |
| `[role="tab"]` 的 `padding` | 官方页签组件的内边距优先级更高 |

**反面清单**：不要全局无脑 `!important`。它会把「状态样式」一起压掉（见 §4.5 的真实事故）。

### 2.2 特异性阶梯（决定「谁压谁」）

注意 `button` 这个语义标签的特异性很低，而限定修饰会迅速抬高：

```
button                         (0,0,1)   ← 语义过载，风险最高
[role="button"]                (0,1,0)
[data-dsh-part="new-session"]  (0,1,0)
button[role="tab"]             (0,1,1)
[role="tab"][aria-selected]    (0,2,0)
[data-dsh-surface="sidebar"] button  (0,1,1)+(0,0,1) → 会压过主操作规则 ⚠
```

**实测教训**：`[data-dsh-surface="sidebar"] button` 这种「区域 + 标签」的写法会**反噬**主操作样式。区域限定不要和 `button` 组合使用。

---

## 3. 整体流程：六步闭环

每一步都给出「怎么算做到」的判据。**跳过任何一步，验证就是假的。**

```
① 定位  →  ② 判定  →  ③ 改  →  ④ 校验  →  ⑤ 截图  →  ⑥ 安装生效
   ↑                                                      │
   └──────────────── 不满意就回到 ① ─────────────────────┘
```

### ① 定位：先确认你要改的元素「是谁」

**不要猜选择器。** 写一个探针，在真实页面里把候选元素连同它的祖先链打出来（标签、`data-dsh-part`、`role`、类名、当前字号/字体/底色）。

判据：**你要改的那段文字出现在探针输出里，且你能说出它的挂载点。**

> 本项目 `tools/shot.mjs` 里的「字号锚点」「字号普查」探针就是干这个的，可直接复用。

### ② 判定：量出来，不要靠看

字号、底色、对比度、尺寸，全部要有数字：

- 字号 / 字体族 → 按「元素 + 字号 + 字体」分组统计（一眼看出层级断层）
- 对比度 → 按 **WCAG AA 正文 4.5:1**（不是 3:1，理由见 §4.1）
- 尺寸 → `getBoundingClientRect()`

判据：**你能说出「改前是 X，我想要 Y」，而不是「感觉有点小」。**

### ③ 改：只写必要特异性

按 §2 判断要不要 `!important`；能用稳定锚点（`data-dsh-part`、`role`、`aria-*`）就不要用哈希类名。

### ④ 校验：契约 + 不变量

至少两项：**清单/契约校验**（文件齐全、路径不越界、字体引用有效）与**配色不变量校验**（对比度、层级顺序）。

判据：**零错误。** 已知且已权衡的取舍要显式豁免并写明理由，不要让它长期刷告警（告警通道一旦被噪声占满，真告警就被淹没）。

### ⑤ 截图：必须用真实 DPR

用无头浏览器截图验证时，**必须强制与真实屏幕相同的设备像素比**，否则字体渲染结论完全不对，验证等于作废。

> 本项目 `tools/shot.mjs` 默认 `DPR 1.25` + `--force-device-scale-factor`，并会自己铸一个本地会话 cookie 去访问真实的 web 端口。

### ⑥ 安装生效

```
改源码 → 构建 → 校验 → 安装到 ~/.dsh/skins/ → 设置→皮肤中心→重开卡片→应用 → 刷新页面
```

**最后一步的「刷新页面」不能省**：皮肤的 CSS/字体是页面加载时注入的，只切设置不刷新，看到的仍是旧皮肤。

---

## 4. 我踩过的坑（症状 / 原因 / 修法）

### 4.1 对比度门槛用了 3:1，漏报了用户能看见的问题

- **症状**：审计全绿，用户仍说「看不清」。
- **原因**：3:1 是**大字号**的门槛；正文要用 4.5:1。
- **修法**：按 4.5:1 重算，且必须按**元素实际所在的抬高面**（surface 第 2/3/4 级）与**选中色**计算，只算最底层底色会漏掉一大半。

### 4.2 全局一刀切改字号，造成「按钮大、正文小」

- **症状**：正文比按钮小一大截（实测正文 14px 对按钮 19.2px，差 37%）。
- **原因**：把**字体族**下得很广，而**字号**只挂在很窄的一组选择器上（`button`、`[role=treeitem]`、`[role=tab]` …），其余元素保留官方自己的 12/13/14px。
- **修法**：字体族与字号**分开决策**。字体族可以全域下发（那是主题身份）；字号要一个个想清楚给谁。

### 4.3 把 `button` 当字号开关，导致元信息比正文还大

- **症状**：底部状态栏「6 轮 427 步 · 94 tok/s」比正文还大、很违和。
- **原因**：**DSH 里 `button` 语义过载** —— 主操作、状态药丸、图标按钮全都是 `button`。你把 `button` 统一抬到 19.2px，官方给整条状态栏定的 13px 就被顶掉了。
- **修法**：字号只给真正的主控件；元信息交回官方阶梯。

> **参照**：本机上另外几套主题（`xp` 只设 11/13px、`minecraft` 与 `summer-liquid-glass` 完全不设字号、`maid-atelier` 12~16px）**一律只换字体族与颜色、字号交回官方阶梯**。想不清该给多少时，先照这个做。

### 4.4 规则挂在错的锚点上，静默不生效

- **症状**：写了规则，正文纹丝不动。
- **原因**：`[data-dsh-part="message-body"]` 这个 part **在正文里不存在** —— 官方实际给段落的是 `message-row`。选择器不匹配不会报错，只是什么都不发生。
- **修法**：一切以探针实测的挂载点为准（§3-①）。

### 4.5 `!important` 顺手把「状态样式」一起压掉了

- **症状**：三个页签长得一模一样，看不出哪个是选中的。
- **原因**：给宽选择器 `[role="tab"]` 加了 `!important` 做扁平化，把选中态的底色和强调下划线一起压掉。**`!important` 层内比特异性**，扁平规则 `(0,1,0)` 压过了选中规则 `(0,2,0)`。
- **修法**：扁平规则用 `:not([aria-selected="true"])` 把选中项排除出去。**给宽选择器加状态覆盖时，永远先问一句：它会不会连带覆盖状态变体？**

### 4.6 截图验证的三个致命细节

- **症状**：预览图与验证数字全部不可信。
- **原因**与修法：
  1. **DPR 不对** → 强制真实 DPR（本机 1.25）。
  2. **一次塞多套皮肤**：皮肤 HTML 是页面加载时注入的，连着切多套只有最后一套渲染，但如果图片写到第一个皮肤目录下，就会「用 A 的图读出 B 的数字」。→ **逐配色验证必须显式指定 id**。
  3. **会话没进去**：页面停在空态也照样截图，于是「验证」了一个根本没有内容页面的状态。→ 进入会话要**带成功判据**（相位不是空态、正文字符数达标），失败要明确报警而不是照拍。

### 4.7 直接切明暗模式，制造出不存在的低对比

- **症状**：亮色下看到一个深色卡片、深色下看到一个亮色卡片，像 bug。
- **原因**：某些元素的底色是**渲染时烘进注入样式表**的；直接改 body 上的主题标记不会触发应用重渲染，颜色会滞后一次切换。
- **修法**：这属于**测量工具的假阳性**，不是皮肤 bug。审计里用「底色与文字亮度都落在页面明暗的反面」这条判据自动标记为「切模式残留」，不计入问题。

### 4.8 用小字清晰度的统计量或视觉模型下结论

- **症状**：改来改去，越改越糊涂。
- **原因**：小尺度像素字体的清晰度，**底色渐变会淹没笔画统计量**，而**视觉模型也会反复看错**（同一个元素，两次描述可以完全相反）。
- **修法**：把候选**并排渲染在同一张物理尺寸的图上，由人眼裁决**。本项目 `tools/font-ab.mjs`、`tools/status-ab.mjs` 就是为此写的。

### 4.9 忘了皮肤表在前，形状被官方覆盖

- **症状**：圆角、底色写了没用。
- **修法**：见 §2.1 的四类必须 `!important` 的属性。

### 4.10 覆盖了不该碰的全屏层

- **症状**：整个界面变暗/变形。
- **原因**：`[data-dsh-surface="overlay"]` 对应一个**常驻的全屏层**（约 1600×1000、`z-index:20`），它平时透明，是给浮层用的容器。
- **修法**：**让它保持透明**。除非你明确要改浮层，否则不要给它加底色或边框。

---

## 5. 像素字体专项：锐利是有条件的

### 5.1 唯一的判据

像素字体（点阵设计）只有在**整数倍**缩放时才锐利：

$$n = \frac{\text{CSS 字号} \times \text{DPR}}{\text{设计档位}} \quad\text{必须是正整数}$$

- `n` 是整数 → 每个设计像素恰好落在整数个设备像素上 → 笔画均匀、锐利。
- `n` 是分数 → 字形起点落在半个设备像素上 → 笔画粗细不均、发虚。

### 5.2 先测你的 DPR，再选字号

**这一步必须在你自己的机器上做**，因为可用档位完全取决于 DPR：

```powershell
# Windows：读屏幕的缩放比例（AppliedDPI ÷ 96 = DPR）
Get-ItemProperty 'HKCU:\Control Panel\Desktop' -Name LogPixels
# 120 → DPR 1.25 ；96 → 1.0 ；144 → 1.5
```

以本机（3840×2160，`AppliedDPI=120` → **DPR 1.25**，设计档位 12px）为例：

| CSS 字号 | 计算 | 结果 |
|---|---|---|
| 9.6px | 9.6×1.25÷12 = 1.0 | ✅ 整数，但太小 |
| 19.2px | 19.2×1.25÷12 = 2.0 | ✅ 整数，主档 |
| 28.8px | 28.8×1.25÷12 = 3.0 | ✅ 整数，太大 |
| 16px | 16×1.25÷12 = 1.67 | ⚠️ 分数，略软 |
| 13px | 13×1.25÷12 = 1.35 | ⚠️ 分数，略软 |

**结论**：这个 DPR 下**只有 19.2px 一档是完全锐利的**（9.6 与 28.8 不实用）。所以「清晰度」与「尺寸合适」是**一对矛盾**，只能权衡，不能两全。

### 5.3 行高也必须落在整数设备像素上

分数行高会把基线推到半个像素上，前面对齐字号档位的努力全白费。

```
行高(CSS) × DPR 必须是整数
16px × 1.75 = 28px → 28×1.25 = 35 ✅
16px × 1.5  = 24px → 24×1.25 = 30 ✅
```

### 5.4 字距与字形参数

像素字体要一并关掉会破坏点阵对齐的东西：

```css
letter-spacing: 0;              /* 非零字距会把字形推离栅格 */
font-kerning: none;
font-variant-ligatures: none;
-webkit-font-smoothing: none;
font-smooth: never;
```

### 5.5 正文字体不要用像素体

长中文段落用像素体阅读成本很高。**正文走可读栈（系统矢量字体），像素体只用于界面外壳。** 并且输入区必须与正文同字体同字号 —— 否则**发送后文字会突变**。

### 5.6 选字体时查许可与覆盖率

本项目用**缝合像素字体 Fusion Pixel 12px**（OFL-1.1，简体覆盖率 100%）。踩过的替代品：Zpix **不是** MIT（个人/教育免费，单个商业产品收费）；Ark Pixel 16px 上游已弃用；ChillBitmap 16px 虽 OFL 但简体覆盖不足。

**覆盖率必须实测**：拿界面里真实出现过的字符集去比对，而不是看字体说明。

---

## 6. 字号怎么定：本项目的最终答案

把上面所有教训收敛成一套规则：

> **字体族全域下发，字号只给主控件，其余交回官方阶梯。**

| 层级 | 字号 | 字体 | 挂载点（实测） |
|---|---|---|---|
| 正文 | 17px | 矢量栈 | `[data-dsh-part="message-row"]` + 其内 `p/li/blockquote` |
| 侧栏文字 | 16px | 像素体 | `button[data-dsh-part="sidebar-entry"]`、`[role="tree"] > *`、树行内的标题 |
| 页签 / 主操作 | 16px | 像素体 | `[role="tab"]`、`[data-dsh-part="new-session"]` |
| 元信息 | 12~13px | 像素体 | **不写，交回官方**（状态药丸、徽章、面包屑、时间戳） |

**两条原理级要点**：

1. **继承永远输给任何声明。** 所以在容器上设字号，能让「没有自己声明字号」的子元素继承到；而已经自己声明了字号的小徽章、时间戳不受影响 —— 这正是我们想要的。
2. **规则挂错锚点会静默失效**，不报错、不提示（见 §4.4）。

### 顺带一个形态教训：页签是标签，不是按钮

页签曾经被判「有点大、有点违和」。实测发现问题不止尺寸 —— `button` 规则给**每个**页签都加了 2px 描边 + 亮底色，三个并排就成了三个独立的按钮盒子（浅色填充浮在深底上、带凸起质感），而紧邻的筛选胶囊是扁平的。

最终定案：`59×37`、内边距 12px、字号 16px、字距 0、**扁平无描边**，只让选中项留 8% 底色 + 2px 强调下划线。**点按面积（37px 高）仍高于 24px 的可点下限**，所以没有退回「太小」。

---

## 7. 复用的验证工具（都在 `源码/tools/`）

| 工具 | 作用 | 什么时候用 |
|---|---|---|
| `shot.mjs` | 真实 DPR 截图 + 探针 | 每次改完必跑；探针负责「定位」与「判定」 |
| `verify.mjs` | 皮肤契约校验 | 构建后必跑，判据是零错误 |
| `audit.mjs` | 配色不变量与对比度 | 改配色后必跑 |
| `font-ab.mjs` | 像素字体候选并排对照 | 纠结字号/字体时，出图给人眼裁决 |
| `status-ab.mjs` | 小字元信息字号对照 | 同上 |
| `inspect-shot.mjs` | 截图放大与锐度度量 | 需要看单像素细节时 |

`verify.mjs` 支持**显式豁免**：在样式里写注释

```css
/* verify-allow: hash-class —— 理由 */
```

一条注释豁免一个 `[class*=]` 选择器。用途是让**已知且已权衡的取舍**不再每次刷告警 —— 但必须写明理由。本项目的哈希类名依赖是历史遗留，能用稳定锚点的地方都已改掉。

---

## 附录：本机实测数据（DPR 1.25，供对照）

| 项目 | 实测值 |
|---|---|
| 屏幕 | 3840×2160，AppliedDPI 120 → **DPR 1.25** |
| 像素字体可用锐利档位 | 9.6 / 19.2 / 28.8 px（12px 设计档） |
| 正文 | 17px，矢量栈，行高 1.65 |
| 侧栏文字 / 页签 / 主操作 | 16px，像素体 |
| 元信息 | 12~13px，交回官方 |
| 页签 | 59×37，内边距 12×5，字距 0，扁平 |
| 圆角 | 操作控件 3px、浮层 4px；列表行/页签/徽章/代码块**方角** |
| 动效 | 180ms `cubic-bezier(.2,.8,.3,1)`，只动 `transform`/`opacity` |

---

## 许可与发布

- **皮肤代码**：MIT。**字体**：缝合像素字体 Fusion Pixel Font © TakWolf，**SIL Open Font License 1.1** —— 二次分发必须保留 `assets/fonts/LICENSE-OFL-font.txt` 与 `NOTICE.txt`。
- 发给别人时的最小文件集见 [安装说明.md](安装说明.md) 第五节。

> 本主题族的全部效果**零 JavaScript**，只靠声明式 CSS —— 因此手工拷贝安装也能完整生效（原因见 §1.2）。